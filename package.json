{
  "name": "employee-backend",
  "version": "1.0.0",
  "description": "Backend API using json-server",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --static false"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
